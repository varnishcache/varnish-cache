#!/bin/sh
#
# $Id$
#

set -ex

./autogen.sh

CONFIG_SHELL=/bin/sh \
./configure \
    --enable-developer-warnings \
    --enable-debugging-symbols \
    --enable-dependency-tracking \
    --enable-werror \
    --prefix=/opt/varnish
